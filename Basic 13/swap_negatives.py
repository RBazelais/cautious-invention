#swap out the negatives
def swap_negatives(arr):
    # if arr == null:
    #     return -1
    # for idx in arr: Pulls the value in the array rather than the actual index
    for idx in range(0, len(arr)): # takes in range of 0 to the array length -1
        if arr[idx] < 0:
            arr[idx] == 'Dojo'
    print arr

swap_negatives([1,2,3,4,5,6])
